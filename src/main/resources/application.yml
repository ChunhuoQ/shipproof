server:
  port: ${PORT:8080}
  compression:
    enabled: true

spring:
  application:
    name: shipproof
  jackson:
    serialization:
      write-dates-as-timestamps: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
